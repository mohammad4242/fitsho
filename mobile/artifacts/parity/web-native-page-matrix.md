# Web ↔ Native page matrix

Phase 0 inventory captured from the checked-out web and native source. Web owns page
information architecture and visual hierarchy. Native owns phone interaction, safe areas,
navigation, keyboard/camera/media behavior, caching, and accessibility.

## Evidence limits

This is a source-and-test inventory. No Android device or emulator was available, so the
visual width and state columns are implementation targets and source-observed behavior, not
physical-device acceptance. Every page remains `implemented-awaiting-device-check` until the
final device matrix is exercised.

Classification values follow the master plan: `VISUAL_HIERARCHY`, `SECTION_ORDER`,
`TYPOGRAPHY`, `SPACING`, `COMPONENT_SHAPE`, `COPY`, `STATE_PRESENTATION`,
`NATIVE_ADAPTATION`, `MISSING_EXISTING_CAPABILITY`, `FUNCTIONAL_BUG`, and `OUT_OF_SCOPE`.

## Route and composition matrix

| Area / phase | Web route and visual source | Native route and current implementation | Actions and states to preserve | Current difference / planned allowlist |
| --- | --- | --- | --- | --- |
| Public entry / 17 | `/` — `PublicLandingPage.tsx`, `publicLanding.css`, `landingStory.css`, `CinematicStory.tsx`, `ProcessStory.tsx`, `BodyIntelligence.tsx`; brand/hero/promise/CTA/process story | `/(public)/index.tsx` → `PublicEntryScreen`; dark `CinematicSurface` poster, title, pills, CTAs | Public onboarding, sign in, register; static loading is route-level | `VISUAL_HIERARCHY`, `COPY`, `NATIVE_ADAPTATION`; modify `mobile/app/(public)/index.tsx` only |
| Public onboarding / 19 | `/get-started` — `PublicOnboardingPage.tsx`, `GuidedSharedProfileQuestions.tsx`, `GuidedTrainingQuestions.tsx`, `publicOnboarding.css`; guided questions and real progress | `/public-onboarding` → `PublicOnboardingScreen`; staged draft form and account handoff | Draft persistence, resume, validation, mode selection, sign in/register handoff, complete state | `SECTION_ORDER`, `STATE_PRESENTATION`, `NATIVE_ADAPTATION`; modify `mobile/onboarding/PublicOnboardingScreen.tsx` and presentation-only helpers |
| Sign in / 18 | `/login` — `AuthShell.tsx`, `LoginPage.tsx`, auth styles; compact heading, method choice, fields, provider, alternative link | `/auth/sign-in`; `AuthScaffold`, two form modes, Google action, notices | Email/password, phone OTP, Google, session-expired/startup/error/loading, onboarding source, register/forgot routes | `COMPONENT_SHAPE`, `SPACING`, `NATIVE_ADAPTATION`; modify auth scaffold/styles and route presentation |
| Register / recovery / verify / 18 | `/register`, `/forgot-password`, `/reset-password`, `/verify-email` — corresponding auth pages and shared CSS | `/auth/register`, `/auth/forgot-password`, `/auth/reset-password`, `/auth/verify-email`, plus `/auth/phone-otp` | Validation, one-time tokens, password confirmation, email verification, resend/status, safe provider/auth errors | `COMPONENT_SHAPE`, `COPY`, `STATE_PRESENTATION`; modify only presentation surfaces |
| Authenticated onboarding / 20 | `/onboarding` — `OnboardingPage.tsx` and profile field components; effective profile question order | `/onboarding/onboarding` → `OnboardingScreen`; stage forms with controller/draft | Draft recovery, profile API, validation, product mode, resume, completion route, Back | `SECTION_ORDER`, `NATIVE_ADAPTATION`; modify `mobile/onboarding/OnboardingScreen.tsx`/presentation mapping only |
| Home / 3 | `/dashboard` — `DashboardPage.tsx`, `dashboard.css`; compact welcome, primary workout card, nutrition card, quick actions | `/member` → `MemberHomeScreen`; compact `PageHeading` welcome, workout media card, quiet nutrition summary card, media quick-action cards | Profile read, workout/nutrition/estimate/tracking queries, product modes, routes, loading/empty/error/offline/stale | Phase 3 complete in `mobile/home/*`: web order is preserved, nutrition no longer uses cinematic surface, 360/390/430 layout contracts remain covered; physical visual check pending |
| Workout plan / 4 | `/workout-plan` — `WorkoutPlanPage.tsx`, `workoutPlan.css`, `WeeklyCheckInCard.tsx`, `EndCycleFeedbackCard.tsx`, prescription formatter; title/duration/context → review → generation → week/days → secondary tools | `/member/workouts` → `WorkoutPlansScreen`, `WorkoutCyclePanel`; Phase 4 complete: web-order hierarchy, shared `SegmentedControl`, media-first next-session day, compact secondary days, native touch targets | Active/pending/rejected/historical, generation method, coach review, exercises, replacement, check-in, PDF/history, offline/cache | `VISUAL_HIERARCHY`, `SECTION_ORDER`, `COMPONENT_SHAPE`, `COPY`; physical visual check pending |
| Exercise catalogue / 5 | `/exercises` — `ExerciseCatalogPage.tsx`, `exercises.css`; search → region → muscle → focus → filters → results | `/member/exercises` → `ExerciseCatalogScreen`; Phase 5 complete: search/guided order, shared stage switcher, secondary Sheet, responsive media cards, active-filter removal | Search params, filters, pagination, cache/list position, media fallback, empty/error/offline | `SECTION_ORDER`, `SPACING`, `STATE_PRESENTATION`; physical visual check pending |
| Exercise detail / 6 | `/exercises/:slug` — `ExerciseDetailPage.tsx`, `exercises.css`, web carousel; media/presentation/name → overview → instructions → safety | `/member/exercises/:slug` → `ExerciseDetailScreen`; Phase 6 complete: compact breadcrumb/back, media-first 4:3 card, selector/title order, direction-aware disclosures | Swipe/count, gender presentation, video lifecycle/cache, unknown/error/loading/offline, Back, language | `VISUAL_HIERARCHY`, `SECTION_ORDER`, `COMPONENT_SHAPE`; physical visual check pending |
| Nutrition overview / 7 | `/nutrition-estimate` — `NutritionEstimatePage.tsx`, `nutritionEstimate.css`; Today/Nutrition heading and tools → real target/ring/macros → plan/clinical/secondary | `/member/nutrition` → `NutritionFoundationScreen`; compact PageHeading, two primary daily tools, real summary/card, plan shortcut, then profile/safety/estimate/plan/tracking/catalogue/clinical composition | Real values, safety blocks, physician requirement, estimate, tracking, catalogue, adherence, clinical tools, cache/offline | Phase 7 complete: Web first-viewport order is adapted to native two-column touch cards; dedicated plan/tracking/catalogue routes and all deeper data/state sections remain; physical visual check pending |
| Nutrition plan / 8 | Plan area in `NutritionEstimatePage.tsx`, `WeeklyNutritionPlan` and nutrition CSS; status/comparison → budget/ideal → selected day/meals → rebuild → shopping | `/member/nutrition-plan` → `NutritionPlanSection`; status-first PageHeading/plan card, selectable vertical budget/ideal comparison, day/meal cards, replacement Sheets, PDF/history | Budget/ideal/active selection, approval, regeneration fallback, replacements, shopping list, PDF/history, offline | Phase 8 complete: plan role/date/approval context and real macro comparison are visible; internal warning/reason codes are mapped to safe copy; native stacked selection remains usable at 360 dp; physical visual check pending |
| Nutrition tracking / 9 | `/nutrition-tracking` — `NutritionTrackingPage.tsx`; Today/Nutrition heading → logged versus planned daily panel → food photo entry → meal/check-in/manual tools → entries | `/member/nutrition-tracking` → `NutritionTrackingSection`; compact PageHeading, real estimate targets beside logged calories/protein, photo entry before lower-frequency tools | Date semantics, decimal handling, duplicate guard, photo/manual entry, offline restrictions, cache invalidation, camera/gallery consent and confirmation | Phase 9 complete: web daily hierarchy and real target state are surfaced before entry tools; native keeps keyboard-safe numeric fields, camera/media flow, cached/offline data, check-in, planned adjustments, and entry deletion; physical visual check pending |
| Food catalogue / 10 | `/food-catalogue` — `FoodCataloguePage.tsx`; heading/search/filter/results/detail | `/member/food-catalogue` → `NutritionCatalogueSection initialMode="foods"`; foods mode, search/chips, result cards, detail Sheet | Approved catalogue data, hidden member prices/internal preparation data, search/cache/offline/detail | `SECTION_ORDER`, `COMPONENT_SHAPE`; modify route/section/thumbnail only |
| Meal catalogue / 10 | `/meal-catalogue` — `MealCataloguePage.tsx`; dedicated meals heading/search/results/detail | `/member/meal-catalogue` → `NutritionCatalogueSection initialMode="meals"` | Dedicated mode, meal media/details, prepared-recipe visibility, search/cache/offline/Sheet | `SECTION_ORDER`, `COMPONENT_SHAPE`; modify route/section/thumbnail only |
| Clinical/labs/supplements / 11 | `/nutrition-labs`, `/nutrition-supplements` — dedicated web pages and current nutrition CSS | No dedicated native route; `NutritionClinicalSection` is embedded under nutrition | Physician approval, scientific detail, safety labels, no unapproved recommendation, current API boundaries | `NATIVE_ADAPTATION`; modify `mobile/nutrition/NutritionClinicalSection.tsx`; route-count difference is intentional unless density becomes unusable |
| Body progress/history / 2, 12 | `/body-progress` — `BodyProgressPage.tsx`, `bodyPhotos.css`; latest status/summary → new/resume → timeline/comparison | `/member/(tabs)/body-analysis` → `BodyAnalysisHistoryScreen tabRoot` as progress landing; `/member/body-analysis` remains capture/resume; history/result routes remain direct | Latest result, incomplete resume, timeline, comparison, secure private media, delete confirmation, offline/error/loading; “New analysis” opens capture | Phase 2 complete in `tabs/_layout.tsx`, `tabs/body-analysis.tsx`, and `BodyAnalysisHistoryScreen.tsx`; five destinations are capability-aware; Phase 12 refines visual hierarchy |
| Body capture / 13 | `/body-progress/new` — `BodyPhotoWizard.tsx`, body-photo CSS; preparation → front/side/back → review/submit | `/member/body-analysis` → `BodyAnalysisWizard`, `BodyAnalysisRequirements`, `BodyPhotoCapture`, Ghost overlay | Native camera/permissions, crop/privacy line, pose validation, retake, progress, interrupted resume, upload/retry, Back | `NATIVE_ADAPTATION`, `STATE_PRESENTATION`; modify capture presentation only |
| Body result / 14 | `/body-progress/:sessionId` — `BodyAnalysisResultPage.tsx`, `BodyAnalysisResult.tsx`, body CSS; current V4 result ordering | `/member/body-analysis-result/:sessionId` → `BodyAnalysisResultScreen`, overview card, result cards, private photo strip | Polling/retry, comparison, private media, specialist review, processing/failure, no fake metrics, schema/data retained | `VISUAL_HIERARCHY`, `SECTION_ORDER`, `STATE_PRESENTATION`; modify result presentation only |
| Profile / 15 | `/profile` — `ProfilePage.tsx`, `profile.css`; identity/photo → core metrics → goals/activity → edit sections | `/member/profile` → `ProfileScreen`, `ProfilePhotoControl`; identity plus tabbed section editor | Private photo lifecycle, validation, health/training/nutrition data, draft/save, keyboard, Back | `VISUAL_HIERARCHY`, `SECTION_ORDER`, `NATIVE_ADAPTATION`; modify screen/photo control; API/model frozen |
| More / 16 | `/more` — `MorePage.tsx`, `more.css`; grouped product/account/privacy/specialist/settings/logout links | `/member/more` → `MoreScreen`; profile card and library destination cards | Real routes, capability filtering, coach/physician guards, logout, privacy/delete | `SECTION_ORDER`, `COMPONENT_SHAPE`, `MISSING_EXISTING_CAPABILITY`; modify More and shared GroupedList |
| Account deletion/privacy / 21 | `/delete-account`, `/privacy` — `AccountDeletionPage.tsx`, `PrivacyPolicyPage.tsx`, related CSS | `/account-deletion` → `AccountDeletionScreen`; privacy/delete links open web paths | Re-auth, confirmation, status polling, destructive semantics, privacy source, safe external open | `NATIVE_ADAPTATION`; presentation-only account screens |
| Coach / 22 | `/coach/workouts` — `CoachWorkoutReviewPage.tsx`, `coachWorkoutReview.css`; queue + selected review workspace | `/coach` → `CoachWorkoutReviewScreen`; queue tabs then selected case, editor, save/approve/reject | Backend role access, claim/lease/renew, revision, offline read-only, notes, decisions, errors | `NATIVE_ADAPTATION`, `SECTION_ORDER`; modify screen presentation only; API/model frozen |
| Physician / 23 | `/physician/nutrition` — `PhysicianNutritionReviewPage.tsx`; current page implementation is source; planned CSS path is absent in checkout | `/physician` → `PhysicianNutritionReviewScreen`; queue → case, labs/supplements/meal editor, decision controls | Physician role, stale/offline read-only, medical context, edits, approval/rejection, notes, audit | `NATIVE_ADAPTATION`, `OUT_OF_SCOPE`; inspect current web styles/components before Phase 23; API/model frozen |
| Admin / boundary | `/admin/*` — web admin pages in `frontend/src/features/admin/` | No native admin group or route by design | Admin catalogue/configuration/AI price search/evidence/review remain web-only | `OUT_OF_SCOPE`; do not create native admin screens |

## Equivalent-state coverage

| State | Web evidence | Native evidence | Inventory classification |
| --- | --- | --- | --- |
| Populated | Web pages map real profile, workout, nutrition, body, and specialist data | Query-backed native screens and model tests use real contract-shaped values | Baseline coverage exists; visual device check pending |
| Loading | Route fallback plus page-specific loading/empty branches | `Skeleton`, `StateSkeleton`, query `isPending`, and route guard loading surfaces | `STATE_PRESENTATION` differences are page-specific |
| Empty | Web empty estimate/plan/catalogue/dashboard states | `EmptyState` and feature-specific empty branches | Preserve action direction; do not replace with fake data |
| Error/retry | Web error sections and retry links/buttons | `Notice` plus refetch/retry actions in feature screens | Preserve safe user-facing copy and retry behavior |
| Offline/stale | Web has page-specific status; native has connectivity monitor and `getMobileViewState` | Native offline/stale notices preserve cached data where available | Native adaptation is required; do not replace page with warning-only UI |
| Pending approval/read-only | Workout coach and nutrition physician flows in web | Native workout, plan, coach, physician, and result screens model read-only states | Preserve guards and explicit status |
| Historical | Workout version history and body timeline on web | Workout history, body history, nutrition history, private local media | Preserve route/data ownership |

## Phase 0 gate

Every core page now has a row with web source, native route/implementation, preserved actions and
states, known differences, and a phase-specific allowlist. No page rebuild should begin without
updating its row with before/after evidence and the exact changed files.
