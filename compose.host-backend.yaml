services:
  hostdev-db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: fitsho
      POSTGRES_PASSWORD: fitsho
      POSTGRES_DB: fitsho
    ports:
      - "5433:5432"
    volumes:
      - fitsho_hostdev_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fitsho -d fitsho"]
      interval: 2s
      timeout: 5s
      retries: 30

  hostdev-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    init: true
    environment:
      VITE_API_PROXY_TARGET: http://host.docker.internal:8002
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "5174:5174"
    volumes:
      - ./frontend:/app
      - fitsho_hostdev_frontend_node_modules:/app/node_modules

volumes:
  fitsho_hostdev_postgres_data:
  fitsho_hostdev_frontend_node_modules:
