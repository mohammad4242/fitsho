# Session Muscle Coherence Implementation Plan

1. Add failing policy, builder, repair, validation, and unequal-count regressions.
2. Implement the centralized coherence policy and reuse it from focus compatibility and
   session ordering.
3. Integrate dynamic and template construction with exact direct-muscle scope.
4. Reverse priority and volume placement toward existing intended exposures.
5. Make duration repair fill primary, secondary, then accessory blocks and expose repair trace.
6. Enforce coherence in recovery and validation.
7. Remove weekly redistribution, its Final Gate requirement, report consumers, and obsolete
   tests.
8. Audit canonical and active DB templates; repair proven metadata inconsistencies through the
   canonical synchronization path if required.
9. Run focused, full Program Engine, lint, type, generation-matrix, and 20-profile checks.
10. Review all delegated diffs, make integration corrections, commit with an explicit allowlist,
    and push `main`.
